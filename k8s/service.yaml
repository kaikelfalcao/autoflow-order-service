apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: autoflow
spec:
  selector:
    app: order-service
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
  type: ClusterIP
